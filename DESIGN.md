# 디자인 시스템 — 윤미래 포트폴리오

이 문서는 포트폴리오의 **단일 디자인 기준(Single Source of Truth)**이다. 기존 화면 수정, 새 섹션·프로젝트·상세 페이지 추가, 반응형 작업을 할 때 가장 먼저 확인하고 실제 코드와 함께 갱신한다.

## 1. 브랜드 무드

### 핵심 키워드

- 무채색
- 깔끔함
- 심플함
- 정돈됨
- 절제된 자신감
- 이미지 중심
- Apple처럼 명확한 강조와 넉넉한 여백
- 에디토리얼 포트폴리오처럼 읽기 쉬운 정보 위계

사이트가 스스로 화려해지는 것이 아니라 **윤미래의 프로젝트 이미지가 가장 돋보이도록 조용하고 정돈된 틀**을 만든다. 강조는 색상이나 장식보다 크기, 굵기, 명암, 여백으로 해결한다.

### 시각적 원칙

1. 한 화면에서 가장 중요한 요소는 하나만 둔다.
2. UI는 흰색·검정·회색만 사용하고 프로젝트 원본 이미지가 색상 강조를 담당한다.
3. 여백은 남는 공간이 아니라 정보 위계를 만드는 요소로 사용한다.
4. 카드보다 콘텐츠를 강조한다. 불필요한 테두리, 그림자, 배지는 사용하지 않는다.
5. 모든 텍스트는 빠르게 훑을 수 있도록 짧고 명확하게 구성한다.
6. 애니메이션은 콘텐츠 이해를 돕는 전환에만 제한적으로 사용한다.

## 2. 디자인 토큰

### 폰트

기본 서체는 모든 페이지에서 **Pretendard**를 사용한다.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css" />
```

```css
font-family: "Pretendard", -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", sans-serif;
```

| 용도 | 굵기 | 크기 | 행간/비고 |
|------|------|------|-----------|
| Hero 제목 | 600 | `clamp(4rem, 7vw, 7.5rem)` | `1.3`, 자간 `-0.055em` |
| 섹션 제목 | 600 | `clamp(2rem, 3.333vw, 3rem)` | 최대 48px, Projects·Experience 공통 |
| Experience 강조문 | 500 | `clamp(2.4rem, 4vw, 4.6rem)` | `1.08`, 자간 `-0.055em` |
| About 강점 제목·본문 | 500~650 | `17px` | 제목은 검정, 설명은 회색 |
| 프로젝트 제목 | 600 | `1.0625rem` | 17px, 행간 `1.3` |
| 프로젝트 타입 | 400 | `0.875rem` | 14px, `--muted` |
| 본문 | 400 | `1rem` | 기본 행간 `1.45` |
| 작은 라벨·메타 | 500~600 | `0.8~0.9rem` | 필요할 때만 사용 |

- 큰 제목은 `letter-spacing: -0.055em`으로 조이기
- 라벨류는 넓히기(대문자 + 자간)

### 컬러

```css
:root {
  --bg:     #ffffff;  /* 기본 배경 */
  --surface:#f5f5f7;  /* 보조 섹션·이미지 배경 */
  --fg:     #1d1d1f;  /* 본문 텍스트 */
  --muted:  #6e6e73;  /* 보조 텍스트 */
  --line:   #eeeeee;  /* 모든 구분선 및 GNB 테두리 */
}
```

- 새로운 UI 포인트 컬러를 임의로 추가하지 않는다.
- `--fg`는 제목·주요 링크, `--muted`는 설명·프로젝트 타입·메타 정보에 사용한다.
- `--surface`는 Contact와 이미지 placeholder 등 보조 면에만 제한적으로 사용한다.
- 프로젝트 이미지의 원래 색상은 유지한다.
- 텍스트와 배경의 대비는 WCAG AA 이상을 확보한다.

### 레이아웃 토큰

- 메인 콘텐츠 폭: 최대 1600px. 좌우 여백은 데스크톱 80px, 태블릿 48px, 모바일 16px로 단계적으로 줄인다
- 데스크톱 섹션 사이 간격은 160px로 통일하기 위해 인접 섹션의 위·아래 패딩을 각각 80px로 사용
- 모바일은 인접 섹션의 위·아래 패딩을 각각 60px로 줄여 총 120px 간격을 사용
- 메인 섹션 사이는 구분선을 사용하지 않고 충분한 세로 여백만으로 분리
- 데스크톱 프로젝트 그리드: 3열, 가로 20px·세로 72px
- 태블릿(≤1024px): 프로젝트 2열, Hero·Experience 1열 전환
- 모바일(≤720px): 프로젝트 1열, 페이지 좌우 여백 16px

## 3. 컴포넌트 규칙

### GNB

- 첫 화면에서는 배경과 구분선이 뷰포트 전체 너비를 채운다.
- 로고와 메뉴는 본문과 동일한 좌우 정렬선을 사용한다.
- 스크롤 후에는 상단 중앙에 최대 1040px, 22px 라운드의 플로팅 GNB로 전환한다.
- 배경은 반투명 흰색과 blur를 사용하며 그림자는 추가하지 않는다.
- 메뉴 순서는 `About → Projects → Experience → Contact`로 유지한다.
- CAI 콘셉트의 좌측 메뉴에서는 `Work`와 `About`을 중복 표기하지 않고 `Experience`만 제공한다.
- CAI 콘셉트의 `Home`은 별도 페이지로 이동하지 않고 우측 프로젝트 스크롤 영역을 최상단으로 이동시킨다.
- CAI 콘셉트의 `Experience`는 좌측 사이드바와 캐릭터를 유지한 채 우측 프로젝트 영역만 경력 화면으로 교체하며, `Home`을 누르면 프로젝트 목록으로 복귀한다.
- CAI Experience 화면은 상단에 3개 핵심 역량을 배치하고, 하단에는 회사별 기간·소개와 프로젝트 상세를 2열 에디토리얼 구조로 제공한다.

### Hero

- 히어로에는 대형 제목, 2줄 소개, Resume 링크와 3개의 핵심 강점을 통합한다.
- 제목 문구는 `안녕하세요. / 디자이너 윤미래입니다.`를 기본으로 한다.
- 장식 이미지, 배지, 불필요한 버튼은 추가하지 않는다.
- 소개문은 데스크톱에서 의도한 두 줄을 유지하고 모바일에서는 자연스럽게 줄바꿈한다.
- 이력서 링크는 소개문 아래에 배치한다.

### About

- Hero 바로 다음, Projects 이전에 배치하여 프로젝트를 보기 전에 디자이너의 핵심 강점을 빠르게 전달한다.
- About에서는 상단 소개·Resume 영역과 01번 강점 사이에만 오른쪽 콘텐츠 열 너비의 구분선 하나를 사용한다.
- 구분선 위·아래 여백은 데스크톱과 모바일 모두 20px로 맞춰 두 영역의 중앙에 배치한다.
- About 상단은 제목과 `짧은 2줄 소개문 → Resume ↗`를 2열로 배치하며, 오른쪽 콘텐츠 정렬선에서 시작한다.
- Resume 링크에는 밑줄이나 버튼 배경을 쓰지 않고 회색 화살표만 덧붙인다.
- 각 항목 안쪽은 Experience와 동일한 `minmax(180px, .7fr) / minmax(0, 1.3fr)` 2열과 `8vw` 간격을 사용하며 콘텐츠는 오른쪽 열에서 시작한다.
- 오른쪽 콘텐츠는 `번호 → 굵은 검정 제목 → 회색 설명` 순서로 줄을 나누어 세로 배치한다.
- 번호와 제목 사이는 8px, 제목과 설명 사이는 20px로 구분한다.
- 강점 번호는 14px Medium(500)를 사용한다.
- 각 강점 행의 세로 패딩은 위 8px, 아래 24px로 사용한다.
- 제목은 20px SemiBold(600), 설명은 16px Regular(400)·행간 1.6으로 구성하고 카드 배경과 그림자는 사용하지 않는다.
- 현재 핵심 축은 `데이터 중심 서비스·LLM AI 검색`, `글로벌·다국어 시스템`, `디자인 시스템·AI 기반 생산성`이다.

### 섹션 제목

- Projects와 Experience는 동일한 `.section-label` 스타일을 사용한다.
- 최대 48px, Pretendard 600, 검정, 자간 `-0.045em`을 사용한다.
- `Selected`, 번호형 라벨, 기간 텍스트를 제목 옆에 추가하지 않는다.

### 프로젝트 카드

- 썸네일 프레임은 모든 카드에서 `16 / 10` 비율로 고정하고 모서리는 4px을 사용한다.
- 실제 이미지는 프레임 안에서 `object-fit: cover`로 전체 면을 채우며, 비율 차이로 인한 최소한의 크롭은 허용한다.
- 핵심 제목·UI가 잘리지 않도록 카드별 `object-position`만 미세 조정할 수 있으며, 이미지 고유 비율이 카드 높이를 변경해서는 안 된다.
- 이미지 아래 20px 간격을 두고 프로젝트 제목과 타입만 표시한다.
- 프로젝트 제목은 17px 검정, 타입은 14px 회색이다.
- 카드에 연도, 장문 설명, 테두리, 그림자를 추가하지 않는다.
- hover는 이미지 `scale(1.015)` 이내로 제한한다.
- 새 카드도 기존 카드와 같은 정보 순서와 썸네일 비율을 사용한다.

### Experience

- 제목은 Projects와 같은 디자인을 사용한다.
- 별도의 대형 소개 문장은 사용하지 않는다.
- 숫자 번호는 사용하지 않는다.
- 오른쪽 열의 각 경력은 네이티브 `details/summary` 아코디언으로 구성한다.
- 닫힌 상태에서는 회사명·재직 기간·회사 소개만 보여주고, 클릭하면 상세 업무를 펼친다.
- 아코디언 위계는 About과 맞춰 회사명 20px SemiBold 검정, 세부 업무 제목 16px Medium, 기간·소개·세부 내용 16px Regular로 구분한다.
- 회사명과 회사 소개 사이의 세로 간격은 20px로 둔다.
- 펼친 상태에서는 회사 소개와 첫 세부 업무 사이의 총 간격을 32px로 둔다.
- 세부 업무 제목은 검정 `--fg`, 본문은 `--muted`를 사용한다. 본문 행간은 1.6으로 두고, 제목과 내용 사이는 4px, 연속된 세부 내용끼리는 추가 간격 없이 배치한다.
- 상세 영역에는 팀명과 직책을 별도로 노출하지 않는다.
- Experience 목록의 첫 항목 위와 마지막 항목 아래에는 선을 두지 않고, 회사 사이에만 `--line` 1px 구분선을 사용한다.
- 기본 상태는 모두 닫힌 상태로 두어 페이지 길이를 줄인다.
- 경력 순서는 `블루밍비트 → 트레드링스 → 디버 → 보내다`로 유지한다.
- 이력서 링크는 Experience에 중복 배치하지 않고 About 상단에서만 제공한다.

### Contact

- 밝은 회색 `--surface` 배경의 큰 마무리 영역으로 구성한다.
- Contact 박스의 네 모서리는 모두 8px radius를 사용한다.
- Contact 박스는 데스크톱과 모바일 모두 약 400px 높이로 간결하게 유지한다.
- `Get in touch` 라벨과 직함·지역 정보는 두지 않는다.
- 박스 내부 상단에 Projects·About과 동일한 `.section-label` 스타일의 `Contact` 제목을 배치한다.
- 이메일은 원문 `alfo2027@naver.com`, 전화번호는 `01057045376`으로 표시한다.
- 이메일은 `mailto:alfo2027@naver.com`, 전화번호는 `tel:01057045376`으로 연결해 클릭 즉시 해당 기능을 사용할 수 있게 한다.
- 두 연락처 링크는 20px Medium으로 박스 내부 하단에 나란히 배치하고 회색 화살표를 덧붙인다.

### 프로젝트 상세

- 별도의 문제 정의·과정·결과 UI를 만들지 않는다.
- 사용자가 제공한 Figma/PDF 포트폴리오 이미지를 위에서 아래로 연속 배치한다.
- 이미지는 AVIF로 변환하고 원본 비율과 충분한 해상도를 유지한다.
- 이미지 사이에는 여백이 없어야 하며 필요하면 1px 겹침으로 흰 선을 방지한다.
- 상세 이미지는 좌우 빈 공간 없이 화면 전체 너비를 채운다.
- 상단에는 프로젝트 목록 이동만 최소한으로 제공한다.
- 상세 이미지 마지막에는 `다른 프로젝트` 영역을 두고, 같은 회사의 공개 프로젝트를 먼저 배치한 뒤 부족한 수만큼 전체 프로젝트 목록 순서로 채워 총 4개를 노출한다.
- 다른 프로젝트는 데스크톱 4열, 태블릿·모바일 2열의 16:10 썸네일 카드로 구성하며 프로젝트명과 타입을 함께 표시한다.

## 4. 인터랙션

- 기본 전환 시간은 200~420ms 범위로 사용한다.
- easing은 `cubic-bezier(.22, 1, .36, 1)`을 우선 사용한다.
- 움직임은 GNB 전환, 외부 링크 화살표, 썸네일 1% 확대에만 사용한다.
- CAI 콘셉트는 마우스와 hover를 지원하는 정밀 포인터 환경에서만 30px 원형 커스텀 커서를 사용한다.
- CAI 프로젝트 링크에서는 커서가 `VIEW ↗` 캡슐로, 일반 링크에서는 화살표 원형으로, 웨스티 캐릭터에서는 `DRAG ↔` 캡슐로 전환한다.
- 커서는 스트로크 없이 62% 반투명 미드그레이 표면과 8px 배경 블러를 사용하고 위치 지연 없이 포인터를 즉시 따른다. 원형과 캡슐 사이 형태 전환만 220ms로 유지한다.
- 프로젝트 hover 시 기존 이미지의 컬러가 살아나고 갤러리 크롭이 미세하게 확대되며, 제목 영역을 오른쪽으로 4px 이동한다. 상세 준비 중인 프로젝트에는 `VIEW ↗` 상태를 사용하지 않는다.
- 터치·모바일 환경과 `prefers-reduced-motion`에서는 커스텀 커서를 숨기고 네이티브 커서를 유지한다.
- 모바일 CAI 홈은 상단에 Home·Experience 메뉴와 소개글·웨스티를 함께 노출한 뒤, 아래에 1열 프로젝트 목록을 이어 붙인다.
- 모바일에서는 데스크톱 전용 내부 스크롤 기반 등장·패럴랙스 효과를 실행하지 않아 모든 프로젝트 썸네일을 즉시 표시한다.
- CAI 콘셉트의 사이드 캐릭터는 기존 드래그 회전·포인터 반응·스크롤 바운스를 유지한다.
- 캐릭터에 마우스를 올리거나 키보드 포커스하면 오른쪽에 짧은 자기소개 말풍선을 표시하고, 포인터 또는 포커스가 벗어나면 숨긴다.
- 말풍선은 현재 배경·텍스트·구분선 토큰을 재사용하고 220~320ms의 짧은 페이드·이동으로 캐릭터가 말하는 인상만 더한다.
- `prefers-reduced-motion`에서는 전환을 제거한다.

## 5. 하지 않는 것

- 새로운 포인트 컬러를 임의로 추가하지 않는다.
- 진한 그림자, 유광 효과, 과도한 glassmorphism을 사용하지 않는다.
- 모든 콘텐츠를 카드 박스 안에 넣지 않는다.
- 한 화면에 여러 개의 큰 CTA를 배치하지 않는다.
- 프로젝트 카드에 연도나 긴 설명을 다시 추가하지 않는다.
- 프로젝트 이미지 위에 텍스트를 겹치지 않는다.
- 큰 확대, 튕김, 회전 등 시선을 빼앗는 hover를 사용하지 않는다.
- 페이지마다 다른 라운드·간격·폰트 규칙을 만들지 않는다.

## 6. 새 페이지·컴포넌트 추가 체크리스트

1. 현재 `:root` 컬러와 Pretendard를 그대로 사용했는가?
2. 데스크톱 80px·태블릿 48px·모바일 16px 거터를 따르는가?
3. 제목 크기와 정보 위계가 기존 Projects·Experience와 일치하는가?
4. 강조를 색상보다 크기·굵기·여백으로 해결했는가?
5. 테두리·그림자·배지·설명문을 불필요하게 추가하지 않았는가?
6. 모바일에서 1열과 16px 여백으로 자연스럽게 전환되는가?
7. 외부 링크에는 `↗`, 새 탭, `noopener noreferrer`가 적용됐는가?
8. 애니메이션이 1.5% 확대와 짧은 전환 범위 안에 있는가?
9. 프로젝트 자산은 AVIF이고 이미지 사이에 틈이 없는가?
10. 이번 결정과 실제 수치를 이 문서 및 결정 로그에 함께 반영했는가?

## 7. 유지 관리 규칙

- 디자인 변경은 `DESIGN.md`를 먼저 또는 코드와 동시에 수정한다.
- 코드와 문서가 다르면 현재 승인된 사용자 요청을 기준으로 둘을 즉시 일치시킨다.
- 새 페이지는 기존 디자인 토큰과 컴포넌트 규칙을 복사해 시작한다.
- 레퍼런스는 분위기와 원칙을 참고하되 특정 사이트를 그대로 복제하지 않는다.
- 과거 결정 로그보다 이 문서 상단의 현재 규칙을 우선한다.

## 결정 로그

- 웨스티 프로젝트 인덱스: 우측 프로젝트를 2열·4:3 갤러리로 정리하고, 상세 이미지에서 대표 장면을 다시 선정해 설명이 많은 첫 표지의 반복을 줄임. 모바일은 1열로 전환
- 웨스티 테마 토글: 사이드바 하단에 12px 단색 원형 토글을 배치하고 라이트·다크 모드에서 전경색으로 반전하며 hover 시 미세 확대
- 웨스티 홈 소개글: 이름 아래에 책·전시·감도 높은 공간에 대한 취향과 꼭 필요한 것만 담는 디자인 태도를 세 문단으로 배치
- 기본 홈페이지 전환: 웨스티 좌측 패널과 비대칭 프로젝트 그리드로 구성한 CAI 화면을 `/`에 배치하고 기존 홈은 `/original`에 보존
- CAI 커서 인터랙션: 정밀 포인터에서 대상별 `VIEW ↗`·`↗`·`DRAG ↔` 커서와 프로젝트 제목 미세 이동을 적용하고 터치·모션 축소 환경은 제외

변경 있을 때마다 최신순으로 위에 한 줄씩 추가.

- 프로젝트 상세 인트로 시안: 이미지 전 대형 프로젝트 제목과 회사 소개를 포함한 단일 본문만 배치하고 카테고리·구분 라벨·메타 정보는 사용하지 않음
- CAI 타이포그래피 통일: 전체 서체를 Pretendard로 변경하고 프로젝트 제목 24px·설명 18px(모바일 제목 22px)로 확대
- CAI 모바일 홈 개선: 소개글과 웨스티를 상단에 함께 배치하고 내부 스크롤 전용 애니메이션을 제외해 전체 썸네일 표시 안정화
- CAI Experience 화면: About 메뉴를 제거하고 좌측 영역을 유지한 채 우측만 3개 핵심 역량과 회사별 경력 상세 화면으로 전환
- CAI 내비게이션 정리: 중복된 Work 메뉴를 제거하고 Home 클릭 시 우측 프로젝트 목록 최상단으로 이동
- 프로젝트 상세 추천 방식 개선: 방향 기반 이전·다음 계산 대신 같은 회사 작업을 우선하고 전체 목록 순서로 보완하는 관련 프로젝트 4개를 노출
- 프로젝트 상세 탐색 개편: 기존 이전·다음 텍스트 링크를 총 4개의 썸네일 카드로 변경
- CAI 캐릭터 말풍선: 드래그 회전·스크롤 바운스를 유지하면서 hover·키보드 focus 시 자기소개가 나타나며 접근성 설명 관계와 모션 감소 설정을 지원
- React SPA 전환: Vite·React Router HashRouter를 사용하고 프로젝트·경력 데이터를 단일 모듈로 관리하며 기존 시각 디자인을 유지
- About 문장형 개편: 기존 포지셔닝 문장과 번호형 3열 구성을 제거하고, 검정 핵심 문장과 회색 설명이 이어지는 대형 문단 3개로 변경
- 블루밍비트 알파 카드 보조 문구를 `LLM Search, B2B Crypto Terminal`로 변경
- 프로젝트 썸네일 라운드 조정: 카드 이미지 프레임 모서리 반경을 4px로 변경
- 프로젝트 카드 보조 문구 미세 조정: 크기 13px, 제목과의 상단 간격 4px
- 프로젝트 카드 보조 문구 스타일 조정: 제목과 동일한 `--fg` 컬러를 사용하고 제목과의 간격을 2px로 축소
- 프로젝트 카드 보조 문구 개편: 역할명 대신 각 프로젝트의 도메인·핵심 과업을 쉼표로 구분해 표시
- 프로젝트 6번 추가: `스케줄 데모 이용률 증대`를 6번째 카드로 배치하고, 1번 이미지를 썸네일로 사용하며 상세 6장을 풀블리드·무간격 AVIF로 연결
- 프로젝트 5번 추가: `디자인 시스템 공통화`를 5번째 카드로 배치하고, 1번 이미지를 썸네일로 사용하며 상세 10장을 풀블리드·무간격 AVIF로 연결
- 프로젝트 순서 확정: 3번은 플랜 구매 경험 개선, 4번은 정기 선적 리포트로 배치
- 프로젝트 추가 방식 보정: 기존 카드를 교체하지 않고 새 프로젝트는 다음 번호의 카드·상세 페이지로 추가해 목록을 누적
- About 핵심 강점 추가: Hero 다음에 데이터·AI·확장성의 3개 강점을 3열/모바일 1열로 배치하고 GNB를 `About → Projects → Experience → Contact`로 변경
- 프로젝트 썸네일 프레임 고정: 모든 카드를 16:10·28px 프레임으로 통일하고 `cover`와 개별 `object-position`으로 여백 없이 최소 크롭 적용
- 섹션 구분선 제거: Hero·Projects·Experience·Contact 사이는 선 없이 세로 여백만으로 구분
- 디자인 시스템 문서화: 현재 무채색·Apple식 여백·에디토리얼 위계와 컴포넌트·반응형·금지 규칙·새 페이지 체크리스트를 단일 기준으로 정리
- 섹션 제목 통일: `Projects`와 `Experience`를 같은 최대 48px 반응형 제목으로 적용하고 `Selected` 및 `[01]` 표기를 제거
- 프로젝트 카드 미세 조정: 제목을 17px로 축소하고 썸네일 가로 간격을 20px로 통일
- 프로젝트 카드 정돈: 썸네일 28px 라운드, 제목 20px·타입 14px의 2단 위계와 20px 간격을 적용하고 카드·섹션 헤더의 연도 표기를 제거
- 반응형 좌우 여백: 데스크톱 80px, 태블릿 48px, 모바일 16px의 공통 페이지 거터를 Hero·Projects·Experience·Contact와 GNB 내용 정렬에 적용
- GNB 형태 전환: Hero에서는 전체 너비 bar, 스크롤 이후에는 상단 12px 간격의 1040px 둥근 플로팅 bar로 전환
- 이력서 연결: Surfit 이력서를 Hero 우측의 주요 텍스트 링크와 Experience 라벨 아래 보조 링크로 제공하고 새 탭 이동을 명시
- 무채색 시스템 정돈: Apple식 여백과 명확한 타이포 위계, 16px 프로젝트 이미지, 3열 그리드, 밝은 Contact, 최소 hover를 적용
- GNB 버그 수정: inline 하단 네비게이션은 `scrollY > 0`에서 즉시 숨기고, fixed 상단 네비게이션의 표시 시점은 기존 경계 기준을 유지
- GNB 전환: Hero 아래 inline 헤더와 상단 fixed 헤더를 분리 렌더링하고 `IntersectionObserver`로 표시를 전환. fixed 헤더는 상단 slide·fade 애니메이션 사용
- Hero·GNB: 연락 버튼을 대형 제목 아래로 이동하고 우측에는 보조 텍스트만 배치. GNB는 Hero 직후에서 스크롤 시 상단 sticky
- 메인 재설계: Small Studio 레퍼런스의 대형 타이포·넓은 여백·번호형 섹션 문법을 포트폴리오에 적용하고 Services를 Experience로 변환. 폰트는 Pretendard 유지
- 프로젝트 카드: 레퍼런스처럼 데스크톱 3열 그리드를 사용하고 각 카드에 프로젝트명·연도·프로젝트 타입을 표시
- 정보 구조: 채용 담당자를 위한 UI/UX·프로덕트 디자이너 포트폴리오로, 소개·프로젝트 카드를 갖춘 홈과 피그마 통이미지를 보여주는 최소한의 상세 페이지로 확정
- 스타일: 프로젝트 상세는 Quiet Editorial의 여백·정보 위계를 채택하고 별도의 문제·과정·성과 섹션은 두지 않음

- 레퍼런스 탐색: 기존 페이지는 유지하고 `concepts.html` + 독립 상세 예시 3종(미니멀 에디토리얼 / 볼드 에이전시 / 몰입형 브랜드 스토리)을 선택용 프로토타입으로 추가
- 컬러: Contact 이메일은 블루 대신 회색·작게 / SNS 링크 제거 / 옆에 이미지 추가
- 컬러: 포인트 컬러 코럴 → **블루(#2f6bff)**
- 폰트: **Pretendard** 채택, 밝은(라이트) 톤 확정
